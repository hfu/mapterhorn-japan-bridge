# MONITORING_REQUIREMENTS.md

長時間かかる生成サイクル(aggregation/downsampling/bundle/merge/rsync)を、
人間がその場で全部見ていなくても安全に進められるようにするための、標準的な
モニタリング作法。セッションを跨いで再現できるように、ここに明文化する。

## 1. 基本原則

- **15分ごとに報告する**。作業内容に変化が無くても、資源状況(ディスク・
  メモリ・CPU)の逼迫が無いことを確認・報告する。
- **ETAは可能な限り毎回算出する**。「時間がかかっても構わない」と言われて
  いても、ETAの提示自体は省略しない——見積もりがあることで、進捗が異常か
  正常かの判断材料になる。
- 大きな状態変化(ステージ完了、エラー、異常な遅延、資源逼迫)は次の定期
  報告を待たずに即座に報告する。
- 複数の長時間ジョブが並行している場合、それぞれの進捗を1つの報告にまとめる
  (別々のMonitorタスクを乱立させない——通知が重複・輻輳する)。

## 2. ETAの算出方法(ステージ別)

### 2.1 進捗ログに直接カウンタがある場合(最も簡単)

`downsampling_run.py`は `N / TOTAL` 形式で進捗を出す。直近2点のサンプル
(件数・経過時間)から単純な線形外挿でETAを出す:

```
rate = (count_now - count_prev) / (time_now - time_prev)
eta_seconds = (total - count_now) / rate
```

**注意**: ペースは一定ではない。処理対象の複雑さがズームレベル・地域によって
大きく変わるため(例: 2026-08-30のdownsampling再構築では、5.7秒/件のペースが
突然0.7秒/件まで加速した——child_zoomの浅い段に移ったことが原因)、直近の
サンプルだけで機械的にETAを出しつつ、**大きく変動したら理由を推測して
一言添える**(「ペースが加速/減速した、おそらく〜のため」)。

### 2.2 ログがバッファリングされて見えない場合

`bundle.py`/`merge_japan_bundles.py`等、`print()`の出力先がファイルリダイレクト
されている場合、Pythonの標準出力バッファリング(4-8KB程度)によって、
実際に進捗していてもログファイルには何も現れないことがある。この場合:

- **出力ファイル自体のサイズ増加**を直接ポーリングする
  (`ls -la bundle-store/*.pmtiles` や `stat -f '%z'`)——これはバッファリングの
  影響を受けない
- 複数ファイルが並行して育っている場合、mtimeで「まだ書き込み中か」を判断する
- 参考: 前回サイクルの最終ファイルサイズ・アーカイブ数が分かっていれば、
  「全アーカイブ数のうち何個が出揃ったか」で概算進捗を出せる

### 2.3 rsyncのように独自の進捗表示を持つツール

`rsync --progress`のstderr出力(バイト数・%・転送速度・ETA)をそのままパース
すればよい。rsync自身が出すETAは信頼できることが多いが、**「バイト数は100%
なのに完了しない」状態**(D67で実際に発生)に注意——これはrsyncの送信側が
最終チェックサム再計算のために送信元ファイル全体を再読み込みしている可能性が
高く、`lsof`(送信元ファイルへの読み込みfdが開いているか)と`iostat`
(実際にディスクI/Oが発生しているか)で「ハングではなく正常な最終検証」である
ことを確認できる。

## 3. 資源逼迫の判定基準

| 項目 | 確認コマンド | 正常の目安 |
|---|---|---|
| ディスク空き | `df -h <対象ボリューム>` | 空き容量が今後の書き込み見込み量を十分に上回っているか(単純に%だけでなく、絶対量で判断) |
| メモリ圧力 | `sysctl kern.memorystatus_vm_pressure_level` | `1`(normal)。`2`(warn)以上は要注意 |
| メモリ(補助) | `sysctl vm.swapusage` | swap使用が急増していないか |
| CPU負荷 | `uptime`(load average) | コア数に対して概ね同等〜やや上回る程度は正常(このマシンはマルチコアで、5ワーカー稼働時にload average 5前後は想定内) |

**「Pages free」は使わない**——macOSは積極的にキャッシュするため、空きページ数
だけでは正常/異常の判断がつかない(このセッションで学んだ教訓)。

**ディスク逼迫の予防的判断**: 単に「今空きがあるか」だけでなく、「このステージが
完走するまでにどれだけ消費するか」を見積もった上で判断する。過去実績
(前回サイクルの最終ファイルサイズ等)と現在の空き容量を突き合わせ、危険な
兆候があれば**完走を待たずに早期に警告する**(2026-08-30、`extract_z8plus.py`
実行前にこの見積もりを怠り、TMPDIRが手薄なディスクを指していたことに気づかず
ENOSPCクラッシュを2回起こした)。

## 4. 並行作業時の注意

- 複数の長時間ジョブを同時に走らせる場合、**それぞれが書き込む先が競合
  しないか**を事前に確認する(同じファイル名、同じglobパターンに引っかから
  ないか)。2026-08-30、バックアップファイルを`pmtiles-store`直下に置いた
  ことで`bundle.py`のglobに巻き込まれてクラッシュした実例あり。
- 一時ファイル・デバッグ用の中間生成物は、**本番の作業ディレクトリと衝突
  しない場所**(できれば専用のスクラッチ領域)に置く。使い終わったら速やかに
  削除する——ただし**まだ実行中のプロセスの作業ディレクトリを消さない**よう、
  削除前に必ず対象プロセスが本当に終了しているか確認する(screenセッションの
  有無だけでなく、実際のプロセスID・CPU使用率で判断する)。

## 5. Monitorツールの実務上のコツ

- 長時間ジョブの完了待ちには `Monitor` で `until ! screen -ls | grep -q <name>; do sleep N; done` パターンを使う。単発の完了確認には `run_in_background` + 完了検知の方が軽い。
- SSHセッション(特にCloudflare Access等のトンネル経由)は、他に多数の
  並行SSHセッションがある状況で単発に切断することがある(exit 255)。
  この場合、対象のscreenセッション自体は無事に動き続けていることが多いので、
  **慌てず`screen -ls`で実体を確認してからMonitorを再設定する**。
- Monitorタスクを複数同時に張ると、通知が輻輳して読みにくくなる。1つの
  対象に付き1つのMonitorに絞る。役目が重複したら片方を`TaskStop`する。
- 進捗確認のためのワンショットのコマンド実行(`ls`, `df`, `tail`等)は、
  `Monitor`ではなく素朴な `Bash` 呼び出しで十分——`Monitor`は「完了を待つ」
  「継続的にイベントを拾う」用途に使う。

## 6. このセッションで確立された標準の報告フォーマット

```
tick(HH:MM)まとめ: <ステージ名>の進捗 X/Y(Z%)。ペース<直近間隔での処理数>。
ETA残り約<時間>。ストレージ<空き容量>、メモリ圧力レベル<N>、
load average <範囲>——逼迫なし/要注意。
```

変化が無いtickが連続する場合は簡潔に「変化なし、待機継続中」で済ませてよい
(ユーザーが不在・就寝中等で、直近の状態を毎回詳細に繰り返す必要は無い)。
